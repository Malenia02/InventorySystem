<?php echo "VULNERABLE - PHP is executing!"; ?>
```

**Step 2 — Upload it to your uploads folder**

Manually drop `test.php` into:
```
inventory_system/uploads/test.php
```

**Step 3 — Access it directly in browser**
```
http://localhost/inventory_system/uploads/test.php